{
  "$comment": "~/.claude/settings.json 에 병합한다. 기존 키를 덮어쓰지 말고 병합한다. 전역에는 강제 게이트(deny/block)를 두지 않는다. 강제 게이트는 프로젝트 .claude/settings.json 에만 둔다.",
  "includeCoAuthoredBy": false,
  "permissions": {
    "ask": [
      "Bash(git push:*)",
      "Bash(npm publish:*)",
      "Bash(rm -rf:*)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[전역 세션 지침] 반드시 필요할 때를 제외하고 한글로 답한다. 프로젝트 루트에 CLAUDE.md 와 .claude-memory/ 가 있으면 먼저 확인한다. 큰 작업은 Plan Mode 로 계획을 먼저 확정한다.'"
          }
        ]
      }
    ]
  }
}
